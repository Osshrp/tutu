h1 = t('.edit')
= render 'form', carriage: @carriage, hidden: true
= link_to t('common.show'), admin_carriage_path(@carriage)
| \
= link_to t('common.back'), admin_train_carriages_path(@carriage.train)
