= form_tag update_time_admin_railway_station_path(station), method: :patch do
  td  = datetime_field_tag attr, station.time_in(route, attr), class: 'form-control'
  td  = hidden_field_tag :route_id, route.id
  td  = submit_tag 'Изменить', class: 'btn btn-primary btn-sm'
